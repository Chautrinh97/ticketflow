# Scripts

**Trạng thái:** chưa có script — placeholder scaffold.

Sẽ chứa script tiện ích cho phát triển/CI: seed dữ liệu mẫu (users/events cho môi trường dev), chạy migration, kiểm tra lint/test trước khi commit, v.v. Bổ sung dần khi implement — mỗi script nên có comment ngắn ở đầu file giải thích mục đích và cách chạy.
